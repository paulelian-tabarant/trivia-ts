export class Player {
    public readonly name: string;
    public readonly number: number;

    constructor(name: string, number: number) {
        this.name = name;
        this.number = number;
    }
}